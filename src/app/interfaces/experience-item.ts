export interface ExperienceItem {
    from: string,
    to?: string,
    position: string,
    institution: string,
    institutionURL: string,
    institute?: string,
    instituteURL?: string,
    homepage?: string,
    homepageURL?: string
}